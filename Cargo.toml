[package]
name = "clang-bridge"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Rust FFI bindings to clang-tools-extra via a thin C++ bridge"
build = "build.rs"

[dependencies]

[build-dependencies]
cc = "1"

[dev-dependencies]
serde_json = "1"
tempfile = "3"
